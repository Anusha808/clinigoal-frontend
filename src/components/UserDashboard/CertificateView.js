import React from "react";

const CertificateView = ({ course }) => (
  <div>
    <h3>🎓 Certificate</h3>
    <button>Download Certificate (PDF)</button>
  </div>
);

export default CertificateView;
